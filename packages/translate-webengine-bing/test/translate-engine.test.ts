// import Translate from '@wordway/translate-api';
// import YoudaoWebEngine from '../src';

describe('translate-engine', () => {
  it('works', async () => {
    // const client = new Translate([new YoudaoWebEngine()]);
    // const r = await client.firstEngine.lookUp('hello');
    // console.log(r);
    // expect(r).toEqual(2);
  });
});
